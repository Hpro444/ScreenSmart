# Development dependencies — runtime + data download, exploration, charts, training,
# and the HTTP demo client. NOT needed in the serving Docker image.
-r requirements.txt
matplotlib==3.11.0
seaborn==0.13.2
requests==2.34.2
httpx==0.28.1
lxml==6.1.1
networkx==3.6.1
