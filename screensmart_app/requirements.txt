# Runtime dependencies — everything the screening API needs to SERVE.
# (Transitive deps like scipy, starlette, anyio, h11 are pulled in automatically.)
# For training / data exploration / charts, use requirements-dev.txt instead.
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1
pandas==3.0.3
pyarrow==24.0.0
numpy==2.4.6
scikit-learn==1.9.0
lightgbm==4.6.0
RapidFuzz==3.14.5
jellyfish==1.2.1
Unidecode==1.4.0
joblib==1.5.3
# live sanctions DB + crypto exposure-index reads (sanctions_source="db")
sqlalchemy>=2.0,<2.1
psycopg[binary]>=3.2,<3.3
# streaming worker (screensmart.worker consumes Kafka)
aiokafka==0.12.0
